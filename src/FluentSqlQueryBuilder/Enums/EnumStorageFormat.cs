namespace FluentSqlQueryBuilder.Enums
{
    /// <summary>
    /// Specifies the storage format for enum values in SQL queries.
    /// </summary>
    public enum EnumStorageFormat
    {
        /// <summary>
        /// Indicates that enum values should be stored as integers.
        /// </summary>
        Int32,

        /// <summary>
        /// Indicates that enum values should be stored as strings.
        /// </summary>
        String
    }
}
